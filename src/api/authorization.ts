// 授权机制
import SHA256 from '@/utils/sha256';
import Ajax from '@/utils/ajax';
import { IUserInfo, IAjaxParams } from '@/interface/index';
import authURL from './urls';
class Authorization {
}
const authorization = new Authorization();
export default authorization;
