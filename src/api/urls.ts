// 个人信息
export const userURL = {};
// 系统管理
export const systemManagementURL = {
};

// 测试人员
export const testerURL = {
};

export const commonURL = {
};

// 授权
const authURL = {
};

export default authURL;
