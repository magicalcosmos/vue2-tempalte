import RDropdownMenu from '../RDropdown/src/dropdown-item';

/* istanbul ignore next */
RDropdownMenu.install = function(Vue) {
  Vue.component(RDropdownMenu.name, RDropdownMenu);
};

export default RDropdownMenu;
