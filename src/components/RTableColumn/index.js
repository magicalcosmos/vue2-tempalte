import RTableColumn from '../RTable/src/table-column';

/* istanbul ignore next */
RTableColumn.install = function(Vue) {
  Vue.component(RTableColumn.name, RTableColumn);
};

export default RTableColumn;
